[project]
name = "kronos-inference"
version = "0.1.0"
description = "Kronos foundation model inference service"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
