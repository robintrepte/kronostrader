packages:
  - "apps/*"
  - "packages/*"

# pnpm 11 supply-chain defaults — allow fresh latest stables per project policy
minimumReleaseAge: 0

allowBuilds:
  sharp: true
  unrs-resolver: true

onlyBuiltDependencies:
  - sharp
  - unrs-resolver
